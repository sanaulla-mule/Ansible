- name: Install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      status: present
  - name: start the  nginx 
    ansible.builtin.service:
      name: start nginx
      status: started 
      enabled: yes   
